---
'@vertile-ai/iac': minor
---

Make `iac.json env.metadata` the sole env metadata source, removing legacy per-source `.env.json` and `metadataFile` compatibility while retaining manifest-driven env sync defaults, described package examples, and offline environment validation.
