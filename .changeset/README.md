# Changesets

Use `pnpm changeset` to record package-visible changes.

When changesets reach `main`, the release workflow opens or updates the Changesets release pull request. Merging that release pull request publishes the package.
